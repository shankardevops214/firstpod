apiVersion: v1
kind: Service
metadata:
  name: myajavaapp-svc
  labels:
    app: myjavaapp
spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 30001
    protocol: TCP
  selector:
    app: myajavaapp
